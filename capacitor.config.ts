import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Digi-Milk---Intership-Project-',
  webDir: 'www'
};

export default config;
